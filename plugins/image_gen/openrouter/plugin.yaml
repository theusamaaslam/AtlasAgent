name: openrouter
version: 1.0.0
description: "OpenRouter + Atlas Gateway image generation (chat-completions image output; reference-grounded). Text-to-image and image-to-image."
author: Atlas Agent
kind: backend
requires_env:
  - OPENROUTER_API_KEY
